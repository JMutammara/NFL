# Core
pandas>=2.1
numpy>=1.26,<2.0
pyarrow>=14
scipy>=1.11
scikit-learn>=1.4
pyyaml>=6
joblib>=1.3
tqdm>=4.66
requests>=2.31

# Data source (nflverse / nflfastR access layer)
nfl_data_py>=0.3.2

# Gradient boosting ensemble
xgboost>=2.0
lightgbm>=4.0
catboost>=1.2

# Hyperparameter search (run locally; never inside the training loop)
optuna>=3.5

# Player volatility network (optional; pipeline falls back to a GBM
# heteroscedastic model if torch is not installed)
torch>=2.1

# Tests
pytest>=7.4
